-- ============================================================
-- migration_rental_price.sql
-- Цена за всю аренду (общая сумма за срок). Подённая больше не вводится —
-- система сама делит на дни для перерасчёта и разнесения.
-- Старые аренды с daily_price продолжают работать по-старому.
-- Запускать в Supabase SQL Editor.
-- ============================================================
begin;

alter table public.rentals add column if not exists rental_price integer;  -- сумма за весь срок (×100)

commit;
